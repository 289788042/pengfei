# Codex Handoff - 错位时差

更新时间：2026-06-16

## 项目

- Godot 项目路径：`E:\pengfei`
- Godot 版本：4.6.2
- MCP：Godot MCP Pro v1.14.1
- 当前策略：先修地基，不急着扩玩法；F1 调试系统暂缓，等核心玩法方向稳定后再重做。

## 当前产品方向

这不是单纯数值养成。核心应该是：

- 底层打工女性在城市里进行时间、精力、债务、体面、关系的取舍。
- 玩家早期目标是活下来并理解账单。
- 中期目标是找到爬坡路径：学历、工作、关系、消费陷阱。
- 后期再让债务、家庭、关系、职业选择互相挤压。

现阶段最重要的不是加新 App，而是让早期体验干净：

- 对话期间不要提前跳数值。
- 地点事件结束后背景和状态必须释放。
- 手机弹窗、微信、地图、支付不能互相穿透。
- 玩家每一次点击都应该知道发生了什么、付出了什么、得到了什么。

## 已修过并验证过的基础链路

- 行动次数已废掉，周末主要由精力限制。
- 多条事件链路已经改成“剧情先播放，剧情结束后再显示数值结算”。
- 微信个人聊天返回按钮已修。
- 微信加号菜单已修，王老师上课入口已接入。
- BOSS 弯聘部分 UI 和面试结算链路已修过。
- 春节回合选项已改为延迟结算。
- 地点背景增加 hold/release，避免从地点回家后还弹地点事件。
- UIStateManager 已接入全局对话阻塞，对话/选择期间右侧手机和“结束本周”会禁用。
- 健身房阿伟邂逅加微信闭环已复测：同意后微信出现阿伟、消息 2 条、未读 2、好感 5。
- 阿伟邂逅完成后 `bg_hold` 和 `app_hold` 已正确归零。
- 团美“水光针+热玛吉”支付链路已复测：支付前不提前加数值，剧情后结算。
- 团美实际数值已对齐 UI：水光针+热玛吉颜值 `+15`，鼻综合颜值 `+30`。
- 沈逸书店邂逅接受路径已复测：微信出现沈逸、消息 1 条、未读 1、地点 hold 正确释放。
- 林远健身房邂逅接受路径已复测：阿伟已认识后能轮到林远，微信出现林远、消息 1 条、未读 1、好感 5、地点 hold 正确释放。
- 邂逅条件不足提示已补充：会显示缺哪个属性、需要多少、当前多少。
- 拒绝加微信路径已修：拒绝不加联系人，但会记录 flag，默认 4 周后可重新邂逅，不再被 99 周冷却近似永久封死。
- Galgame 对话结束时不再直接 deferred 解锁“结束本周”，统一交给 UIStateManager 判断，避免加微信选项阶段按钮漏解锁。
- 陈默书店邂逅已复测：沈逸已认识后能轮到陈默，微信出现陈默、消息 1 条、未读 1、好感 3。
- 阿强酒吧邂逅已复测：微信出现阿强、消息 1 条、未读 1、好感 5。
- 许子轩酒吧邂逅已复测：阿强已认识后能轮到许子轩，微信出现许子轩、消息 1 条、未读 1、好感 5。
- 无背景图地点增加 fallback：酒吧等地点不会再直接回 home 背景，而是显示地点专属底色；后续仍需正式背景图。
- 拒绝后 4 周冷却到期已复测：第 12 周不出现，第 13 周能重新遇到沈逸。
- 月末账单和 Game Over 全流程已复测：高风险账单会在确认前显示最低还款失败、预计实还、结算后现金/债务、情绪归零和死亡原因；确认后进入 Game Over，不再被旧对白层压住。
- 正常分期账单已复测：确认后现金 1850、分期债务 11000、剩余 11 期、进入第 2 月第 1 周，不误触发 Game Over。
- 月末账单 UI 已从全屏贴边日志改成居中账单面板，支持短账单/长账单动态高度，并在月末、Game Over、结局入口清理旧对白和手机暗化层。
- 微信 / NPC 重做第一层已完成：联系人现在有 `relation`、`source_label`、`source_detail`、`intro_week`。
- 周姐自动解锁不再无来源空降：第 3 周会显示“新联系人：周姐”、关系、来源和说明；聊天记录顶部有联系人来源系统消息。
- 微信聊天列表、通讯录、朋友圈已接入关系和来源展示；通讯录标题显示 `通讯录(n)`，朋友圈标题显示 `朋友圈(n)`，不再三个 tab 都复用微信未读标题。
- 未配置头像色的 NPC 有稳定 fallback 色；朋友圈不再给没有动态的 NPC 显示 `...` 空占位。
- 微信 / NPC 重做第二层第一刀已完成：`milestones` 关系事件接入微信菜单，达到好感阈值后出现“重要对话”。
- NPC 运行时数据增加 `used_milestones`；`GameManager` 增加总好感计算、下一个可触发里程碑查询、里程碑已处理标记和通用 NPC flag 入口。
- 重要对话支持“边界回复 / 顺着回应 / 暂时不回”；属性不足时边界回复会灰掉并显示需求。
- 重要对话数值时序已验证：聊天气泡先出现，左侧独立结算框后出现，玩家点掉结算后才真正应用数值。
- 回复按钮容器已从 `ChatViewBG` 改挂到 `WCChatView` 浮层，修复动态回复按钮被 `PanelContainer` 拉满并飞出屏幕的问题；日常聊天和里程碑聊天共用底部定位逻辑。
- 小雅 `xy_ms_01 / 闺蜜的真相` 已复测：顺着回应后精力 100 -> 95、情绪 100 -> 95、总好感 30 -> 33，`used_milestones` 记录 `xy_ms_01`，不会重复触发。
- 微信 / NPC 第二层朋友圈互动已接入：`moments` 不再只是展示，玩家可以点击“解读这条动态”。
- NPC 运行时数据增加 `used_moments`；`GameManager` 增加下一个未解读朋友圈查询和朋友圈已处理标记。
- 朋友圈解读支持看穿/误读：属性达标时显示 `truth_lines/pass_note` 并应用 `pass_stat_changes`，属性不足时显示误读文本并应用 `fail_stat_changes`。
- 朋友圈解读时序已验证：先显示解读文本，再显示左侧独立结算框，点掉结算后才应用数值，并把结果写入对应 NPC 聊天记录。
- 沈逸朋友圈已复测：
  - 低情商误读 `moment_01`：情绪 100 -> 95，好感 10 -> 20，下一条推进到 `moment_02`。
  - 高情商看穿 `moment_02`：情绪 80 -> 90，`used_moments` 记录 `moment_02`，flags 记录 `moment_02_pass`，下一条推进到 `moment_03`。
- 2026-06-16 已决定暂缓继续扩微信/NPC 剧情：NPC 具体剧本、人设、主线还需要重新策划，不能继续在空白剧情上堆系统。
- 第一月体验闭环第一刀已完成：主状态区新增“当前目标/本周目标”，按工作日、周末、月底、事件阶段动态切换；危险现金流会覆盖普通提示，优先提示保现金流。
- 关键按钮增加成本/用途 tooltip：餐饮预算、工作态度、支付宝、日记、地图、宝淘、团美、BOSS 弯聘、探探、贝壳等。
- 支付宝和日记已纳入手机网格启用/禁用流程；对话/事件禁用手机时，基础 App 视觉状态也会跟着禁用。
- 修正 `MainGame.gd` 中新 App 解锁提示的跨行字符串，避免潜在 GDScript 解析风险。
- 手机主屏视觉整理第一刀已完成：未解锁 App 默认隐藏，不再以灰暗坏按钮占格；第 1 周只显示地图、微信、支付宝、日记、BOSS，第 3 回合宝淘会渐进出现。
- 手机底部状态区已收紧：压力摘要、成长线、当前目标文案缩短，字号下调，减少信息挤压。

## 关键文件

- `E:\pengfei\scenes\MainGame.gd`
- `E:\pengfei\scripts\ActionService.gd`
- `E:\pengfei\scripts\AppPopupSystem.gd`
- `E:\pengfei\scripts\GalgameSystem.gd`
- `E:\pengfei\scripts\GameManager.gd`
- `E:\pengfei\scripts\UIStateManager.gd`
- `E:\pengfei\scripts\WeChatSystem.gd`
- `E:\pengfei\scripts\SpringFestivalSystem.gd`
- `E:\pengfei\QA_PLAYTEST_3_MONTHS.md`

## 下一步优先级

1. 第一月体验闭环继续推进
   - 玩家第一周进来应该自然知道：手机能干什么、地图能干什么、钱怎么花、债怎么欠。
   - 已有第一版“当前目标/本周目标”、按钮 tooltip、未解锁 App 隐藏，下一步要做真实玩家路径复测：开场 -> 支付宝 -> 地图 -> 餐饮/工作 -> 周末探索 -> 月底账单。
   - 各地点要有明确功能和成本，不只是一次性文本。

2. 手机主屏视觉整理
   - 底部信息区现在能提供目标和财务压力，但空间偏紧，后续应重排为更清晰的信息层级。
   - 未解锁 App 已隐藏；下一步可考虑做解锁动画/新 App 高亮，而不是突然出现。
   - 对话/事件期间右侧手机会暗化，但仍需继续检查弹窗压层和位置轻微跳动。

3. 地点和行动体验
   - 地图地点要继续按玩家乱逛方式复测：进入地点、失败邂逅、条件不足、拒绝、快速跳过、返回后背景释放。
   - 每个地点需要补清楚“为什么去、花什么、得什么、风险是什么”。
   - 酒吧、咖啡厅、公园、夜市目前只有颜色 fallback，后续需要正式背景图。
   - fallback 只用于避免背景回家，不是最终品质。

4. 微信 / NPC 暂缓
   - 不继续接 `offline_dates`，直到 NPC 剧本、人设、主线关系重新定稿。
   - 现有微信系统只作为通讯、上课、基础关系地基保留。

5. 文件管理清理
   - 临时 MCP 截图统一移动或忽略。
   - `_mcp_addon_backups/` 已加 `.gdignore`，后续最好移到项目外。
   - `QA_PLAYTEST_3_MONTHS.md` 保留为正式测试记录。

## 新聊天续接方式

如果上下文快满，或者需要开新聊天，直接发这句话：

```text
请读取 E:\pengfei\CODEX_HANDOFF.md 和 E:\pengfei\QA_PLAYTEST_3_MONTHS.md，继续从下一优先级开始做。
```

上下文满时通常会自动压缩，不一定会立刻丢失；但最稳的办法是让项目里始终有这个交接文档。新聊天不会天然继承所有细节，除非让它读取这里的文档。
## 2026-06-16 首月路径复测补充

本轮已经做完的低级修复：
- `scenes/MainGame.gd`：修复开场 Ctrl 跳过后手机 App 被 UIStateManager 恢复成 disabled 的问题。现在周末无阻塞时 `_repair_next_week_button_state()` 会同步 `_sync_phone_home_apps(true)`。
- `scripts/PhoneAnimation.gd`：标题页提示从“点击手机开始”改成“点击手机弹出”，降低误导。
- `scripts/GalgameSystem.gd`：新增 `_phone_dim_tween` 管理手机暗化遮罩动画，修复 Ctrl 跳过地点剧情后旧 hide tween 把新结算遮罩关掉的问题。

本轮复测通过：
- 图书馆/书店沈逸邂逅：地点背景保持到剧情、结算、加微信请求结束，没有提前回家隔空要微信。
- 沈逸同意加微信后最终进入微信列表；个人聊天页 `<` 返回按钮单击有效。
- 图书馆地点结算、工作日正常打卡都遵守“剧情/事件 -> 独立结算框 -> 玩家确认 -> 应用数值”的时序。
- 第 2 周工作日低价套餐 -> 正常打卡 -> 回第 2 周周末链路通过；套餐按钮、工作按钮、结束本周按钮没有卡灰。

本轮新增需要后续处理的产品问题：
- 图书馆 61 页 + 同意加微信后 43 页，早期探索节奏过重。建议拆成短地点事件，后续内容挪到微信/日记/后续再遇。
- 微信 UI 弹在左中区域，右侧手机仍可见且暗化，视觉上像“双手机”。后续需要统一 App 容器语言。
- 支付宝仍像白色调试面板，不像手机 App。
- 第 1 周地图地点过多，建议渐进开放或分组。
- 城中村家背景资源名仍是 `Assets/title_bg2.png`，文件管理不干净，后续应重命名/迁移引用。
- 工作日全屏面板覆盖时，右侧手机 App 内部状态仍 enabled，虽被遮住但状态机不够干净。

## 2026-06-16 第二刀：手机 App 容器地基

本轮已改：
- `scripts/AlipaySystem.gd`：支付宝打开时运行时搬入右侧 `PhoneScreen`，不再作为根节点全屏白色调试面板；补了手机内遮罩、标题栏、关闭按钮、滚动区、按钮样式、花呗/理财/账单文案。
- `scripts/AppPopupSystem.gd`：新增 `_setup_phone_app_layers()` / `_setup_phone_layer()`，地图、宝淘、团美、贝壳、BOSS、日记、星座、探探、深夜弹窗统一套用手机内全屏层、鼠标拦截和 z_index 规则。
- 通用列表 App 的遮罩和面板边距收紧，关闭按钮改成 `X` 并使用 `pressed` 信号，不再走临时 `gui_input` 关闭。
- 复测后补修：宝淘/团美付款商品不再先关闭 App 页面；支付弹窗压在商品页上方，背景商品页变暗保留。
- 复测后补修：支付宝右上角关闭按钮固定为正常小按钮，不再被布局拉成半行宽。

验证：
- Godot headless 编译通过，项目可加载 `MainGame.tscn` 依赖：8 个 NPC 剧本、125 条职场微事件、8 个地点叙事。
- MCP 恢复后已复测：支付宝只覆盖右侧手机，关闭后桌面和“结束本周”恢复；BOSS 红字竖排未复现；高德地图在手机内显示且拦截底层按钮；宝淘商品支付弹窗正确压在宝淘页上，取消后返回宝淘，关闭后状态恢复。
- `validate_script` 通过：`res://scripts/AppPopupSystem.gd`、`res://scripts/AlipaySystem.gd`。

新增体验债：
- 解锁宝淘会改变 BOSS 图标位置，后续需要固定槽位/分页/新 App 高亮入场。
- 支付宝和手机主屏信息密度仍偏高，下一轮应做可读性重排。

## 2026-06-16 第三刀：重 App 大屏容器 / 支付宝重排

本轮产品判断：
- 不是所有 App 都必须硬塞进右侧手机屏幕。
- 地图、宝淘、团美这类轻量 App 可以继续在手机内运行。
- 支付宝、微信、日记这类信息密度高、需要阅读和复盘的 App，允许从手机入口打开后接管更大的桌面面板。
- 这比“为了拟真而全部挤进手机”更符合真实玩家体验：玩家看账单、聊天记录、日记时需要更大阅读面积。

本轮已改：
- `scripts/AlipaySystem.gd`：支付宝从手机内小弹层改成根节点大屏遮罩面板，打开时背景整体变暗，右侧手机保留为被遮罩的来源。
- 支付宝新增左侧页签：`总览 / 花呗 / 理财 / 账单 / 关于`。
- 总览页显示现金、花呗、月末压力和系统提示。
- 花呗页集中处理欠款、主动还款、分期条件和分期说明。
- 理财页把稳健宝和高风险基金拆开，说明它们不是福利按钮，而是现金流风险选择。
- 账单页显示本月流水判断；无流水时显示空状态，不再留白像没加载出来。
- 关于页加入游戏内说明，告诉玩家每次大额消费后应该看月底预计现金。
- 页签切换时隐藏页会关闭鼠标输入，避免看不见的按钮误触发。

已验证：
- `validate_script res://scripts/AlipaySystem.gd` 通过。
- MCP 运行复测：周末打开支付宝 -> 总览 / 花呗 / 理财 / 账单 / 关于均可切换。
- 在总览页点击隐藏理财按钮重叠区域，现金没有被误扣，隐藏页签不会误触发。
- 关闭支付宝后返回手机桌面，App 按钮和“结束本周”恢复可点。

后续建议：
- 微信和日记也应该走“重 App 大屏”方向，但微信 NPC 剧情未定，先不碰内容系统。
- 支付宝下一轮可以继续补：流水分类筛选、还款记录、花呗利息/最低还款解释、月末压力图标化。
- 大屏容器应抽象成通用 HeavyApp 容器，避免支付宝、微信、日记各写一套。

## 2026-06-16 第四刀：HeavyApp 容器地基 / 日记本大屏

本轮产品判断：
- 日记不是轻量按钮面板，而是玩家复盘工具。它需要大屏阅读面积、筛选、空状态和“最近发生了什么”的清晰顺序。
- 手机仍是入口，但日记打开后可以像支付宝一样接管大屏。
- 微信未来也适合走 HeavyApp，但微信剧情和 NPC 人设未定，本轮不碰微信内容。

本轮已改：
- `scripts/AppPopupSystem.gd`：新增 HeavyApp 通用层 helper：
  - `_setup_heavy_app_layer()`：把重 App 运行时 reparent 到 `MainGame` 根节点，设置全屏遮罩、居中大面板、鼠标拦截和层级。
  - `_is_heavy_app_layer()`：让 `_set_layer_visible()` 能区分手机内小 App 和大屏重 App。
  - `_make_flat_style()`、`_style_text_button()`、`_make_heavy_card()` 等基础 UI helper，后续可供日记/微信继续复用。
- 日记本从手机小面板迁移到 HeavyApp 大屏：
  - 顶部标题栏：`日记本 / 活动流水、数值变化和关键选择复盘`。
  - 本月回顾卡：显示总记录数、当前筛选、分类计数，并说明日记用于复盘。
  - 筛选按钮固定尺寸并有选中态。
  - 日志改为卡片式、最新在上。
  - 无记录或筛选无结果时显示空状态，不再空白。
  - 旧 `BtnCloseDiary`、筛选按钮节点继续复用，原信号仍有效。

已验证：
- `validate_script res://scripts/AppPopupSystem.gd` 通过。
- MCP 运行复测：第 1 周周末打开日记，大屏遮罩和右侧手机暗化正常。
- 运行时插入测试记录后，日记能显示日常/工作/消费/社交记录，最新在上。
- 点击“消费”筛选后只显示消费记录，选中态正常。
- 关闭日记后回到手机桌面，App 图标和“结束本周”恢复可点。

后续建议：
- 把支付宝也逐步迁到同一套 HeavyApp helper，减少重复布局代码。
- 日记下一步可以加入“数值变化摘要”和“关键事件标记”，让它不只是流水，而是玩家的复盘工具。
- 微信等 NPC 剧情定稿后，再用 HeavyApp 容器重做微信，避免现在只做空壳。

## 2026-06-16 第五刀：HeavyApp 公共外壳抽取

本轮目标：
- 把支付宝、日记共用的大屏 App 外壳能力从具体系统里抽出来。
- 这一步是地基整理，不新增微信 NPC 剧情，不改支付宝玩法数值。

本轮已改：
- 新增 `scripts/HeavyAppUI.gd`：
  - `setup_layer()`：统一重 App 的根节点 reparent、全屏遮罩、居中容器边距、面板最小尺寸、鼠标拦截和层级。
  - `detach_from_parent()`：统一安全移除旧父节点。
  - `make_flat_style()`、`style_text_button()`、`make_body_label()`：统一基础样式能力。
- `scripts/AppPopupSystem.gd`：
  - 引入 `HeavyAppUI`。
  - 原 `_setup_heavy_app_layer()`、`_detach_from_parent()`、`_make_flat_style()`、`_style_text_button()`、日记 `_make_body_label()` 改成薄包装，保留原调用点，降低本轮风险。
- `scripts/AlipaySystem.gd`：
  - 引入 `HeavyAppUI`。
  - 支付宝大屏外壳改用公共 `setup_layer()`。
  - 支付宝局部样式 helper 改用公共样式函数。

已验证：
- `validate_script` 通过：
  - `res://scripts/HeavyAppUI.gd`
  - `res://scripts/AppPopupSystem.gd`
  - `res://scripts/AlipaySystem.gd`
- MCP 运行复测：
  - 周末打开支付宝：总览页、账单页显示正常，遮罩和面板层级正常。
  - 支付宝关闭后回到手机桌面。
  - 打开日记：本月回顾、筛选、最近记录显示正常。
  - 点击“消费”筛选：只显示消费记录，选中态正常。
  - 日记关闭后右下角“结束本周”恢复可点。
- Godot 编辑器错误日志为 0。

测试备注：
- MCP 的 `click_button_by_text("×")` 在多个隐藏/显示关闭按钮同名时可能选到隐藏的支付宝关闭按钮。后续自动化测试关闭大屏 App 时优先使用坐标或明确路径，不用重复文本按钮。

后续建议：
- 支付宝总览页下半部分留白偏大，可以补“近三笔流水 / 月底风险条 / 本月最大支出”。
- HeavyAppUI 现在只负责外壳和基础样式；后续微信重做时可以直接用它，但微信内容必须等 NPC 人设和主线方向定稿。
- 下一步可以继续做支付宝信息体验，或者转去主屏状态卡/手机桌面固定槽位，二者都比现在立刻碰微信剧情更稳。

## 2026-06-16 第六刀：支付宝总览页财务复盘

本轮产品判断：
- 支付宝总览不能只是余额和花呗数字，它应该回答玩家最关心的四个问题：
  - 我月底会不会爆？
  - 最近钱花到哪里了？
  - 最大一笔支出是什么？
  - 花呗债务到底有没有被藏起来？
- 右下角“月底”按钮使用真实月末结算预览，花呗按最低还款/分期规则计算。
- 支付宝额外显示“全额还清后现金”，用于提醒玩家债务本金的真实压力。两个数字必须命名清楚，不能都叫“月底预计”。

本轮已改：
- `scripts/AlipaySystem.gd`：
  - 总览页新增“月末风险条”。
  - 总览页新增“近三笔流水”，从本月 `financial_log` 最新记录倒序读取。
  - 总览页新增“本月最大支出”，显示金额、支付方式和提醒文案。
  - 总览页新增“现金流拆解”，显示本月入账、现金支出、花呗支出、可用资金、月底固定待扣、月底结算后现金、全额还清后现金。
  - 花呗详情和月底压力卡改名：区分 `月底结算后现金` 与 `全额还清后现金`。
  - 新增 `_get_month_end_preview_safe()`，复用 `MainGame._get_month_end_preview()`，保证支付宝和右下角按钮的真实月底结算数字一致。

已验证：
- `validate_script res://scripts/AlipaySystem.gd` 通过。
- MCP 压力测试档：
  - 现金 2200，待发工资 1500，餐饮 800，花呗 4200。
  - 右下角显示 `月底:780`。
  - 支付宝总览显示 `月底结算后 780` 和 `全额还清后 -2800`，口径清楚。
  - 风险条显示危险态，近三笔流水、最大支出、现金流拆解正常显示。
  - 切到账单页正常。
  - 关闭支付宝后回到手机桌面，“结束本周”恢复可点。
- Godot 编辑器错误日志为 0。

后续建议：
- 支付宝账单页下一步可以加筛选：全部/现金/花呗/收入。
- 右侧手机主屏的状态卡仍偏密，后续应把“压力、目标、流水摘要”做成更清楚的卡片，而不是一段小字。
- 支付宝目前已能承担财务复盘功能，下一步不必继续深挖支付宝，可转向主屏信息架构或手机桌面固定槽位。

## 2026-06-16 第七刀：手机主屏状态卡信息层级

本轮产品判断：
- 支付宝负责财务明细，手机主屏只负责“一眼判断下一步”。
- 右侧主屏原状态卡太像调试信息：压力、目标、成长、债务都挤成小字，且右侧头像块没有玩法意义。
- 主屏要减少解释，强化方向：下一步做什么、月底是否危险、成长线有没有进度。

本轮已改：
- `scenes/MainGame.gd`：
  - 主屏状态卡隐藏无功能的 `PlayerAvatar`，把宽度还给文字和进度条。
  - 时间文本从 `23岁 | 第1月 | 第1周` 缩短为 `23岁  1月第1周`。
  - 玩家信息从 `姓名:xx | 星座:xx` 改为 `xx · 星座`。
  - `LabelEarlyGoal` 改成主提示，显示为 `下一步｜...`，并提前到状态卡上方。
  - 财务摘要改为 `财务｜压力 月底 X 账单 X 债 X`，和右下角/支付宝口径一致。
  - 成长摘要保留职位、学历、夜校进度，不再重复学识/情商等上方已有数值。

已验证：
- `validate_script res://scenes/MainGame.gd` 通过。
- MCP 运行复测：
  - 安全状态：主屏状态卡无头像占位，进度条宽度恢复，目标/财务/成长三行显示正常。
  - 压力状态：现金 2200、待发工资 1500、餐饮 800、花呗 4200 时，主屏显示 `月底 780`、`债 4200`，无溢出。
  - 点击支付宝入口后，大屏支付宝正常打开，主屏被遮罩压暗。
- Godot 编辑器错误日志为 0。

后续建议：
- 主屏 App 图标仍有“解锁后位置跳动”的肌肉记忆问题，下一刀适合做固定槽位：未解锁 App 显示灰锁占位，而不是动态挤位。
- 状态卡下一步可以继续做视觉小卡片化，但暂时不要把它做成第二个支付宝。

## 2026-06-17 第八刀：手机主屏 App 固定槽位

本轮产品判断：
- 上一版隐藏未解锁 App 虽然让第一周更干净，但解锁宝淘、滑动交友等 App 时会挤动 BOSS、日记等入口，玩家的手感位置会被打断。
- 手机主屏更应该像真实桌面：App 槽位从一开始固定，未解锁时显示灰锁和开放周数，解锁后只改变状态，不改变位置。
- 这解决的是体验稳定性，不是新增玩法。

本轮已改：
- `GameManager.gd` 新增 App 显示名、解锁周数查询和统一解锁提示：
  - `get_app_display_name(app_id)`
  - `get_app_unlock_turn(app_id)`
  - `get_app_unlock_hint(app_id)`
- `MainGame.gd` 的手机 App 状态改为固定槽位：
  - 未解锁 App 不再 `visible=false`。
  - 未解锁 App 显示 `锁定 + App 名称`，并使用灰色半透明视觉状态。
  - 已解锁 App 会恢复原始按钮文本。
  - 对话/事件禁用手机时，已解锁和未解锁 App 都会进入统一禁用透明度，不再出现“看着能点但实际打不开”的割裂感。
- `MainGame.gd` 新增 `_setup_phone_home_layout()`：
  - 3 列固定网格。
  - 缩小 App 按钮到 `104x80`。
  - 降低行列间距和字号，避免 10 个固定槽位挤爆手机主屏。
- `AppPopupSystem.gd` 新增 `_ensure_app_unlocked(app_id)`：
  - 点击锁定 App 时只弹出解锁说明。
  - 地图、日记、宝淘、团美、星座、贝壳、滑动交友、BOSS 都接入了解锁防护。

已验证：
- `git diff --check` 无补丁/空白错误。
- Godot headless 启动 `res://scenes/MainGame.tscn` 5 帧通过，正常加载：
  - 8 个 NPC 剧本
  - 125 条职场微事件
  - 8 个地点叙事
- Godot CLI 临时自动检查通过：
  - 第 1/3/5/9/13/17 周所有 App 按钮 `visible=true`。
  - 所有 App 槽位坐标和尺寸保持不变，`CODEX_PHONE_SLOT_DRIFT=[]`。
  - 第 3 周宝淘可正常打开，`CODEX_UNLOCKED_BAOTAO_VISIBLE=true`。
  - 第 1 周点击锁定宝淘不会打开宝淘菜单，`CODEX_LOCKED_BAOTAO_VISIBLE=false`。
- 当前 MCP 连接仍显示 Godot editor not connected，因此本轮尚未完成截图复测。

后续必须复测：
- 第 1 周周末手机主屏：10 个 App 槽位是否全部稳定显示，灰锁是否挤压状态卡。
- 视觉层确认第 1 周锁定宝淘提示文案是否清楚。
- 临时把 `turn_count` 调到 3/5/9/13/17：截图确认 App 解锁时视觉没有跳动。
- 对话/事件期间主屏按钮禁用透明度是否统一，不能出现锁定按钮比已解锁按钮更像可点的情况。

## 2026-06-17 第九刀：高德地图大屏 App 化

本轮产品判断：
- 地图不是轻量入口，而是周末行动的“决策中心”。
- 玩家打开地图时，需要同时比较地点、成本、收益、条件、当前精力、现金和花呗压力。
- 这些信息硬塞进手机小屏会变成小字调试列表，玩家无法快速判断“去哪、为什么去不了、代价是什么”。
- 所以地图采用大屏信息窗口：手机桌面负责入口，大屏负责路线规划和行动决策。
- 这不代表所有 App 都大屏；宝淘、星座等轻操作仍适合小屏。支付宝、日记、地图、后期微信这类高信息密度 App 才适合大屏。

本轮已改：
- `AppPopupSystem.gd`：
  - 地图从手机内小面板改为 `HeavyAppUI` 大屏遮罩。
  - 地点列表从“按钮行”改为“地点卡片”。
  - 每个地点显示玩法定位、简短用途、消耗、收益、条件和可前往状态。
  - 状态标签统一为 `可前往`、`精力不足`、`本周已去`、`仅周末`、`属性不足` 等。
  - 条件不足时按钮显示 `查看原因`，不隐藏地点，保留探索感。
  - 顶部显示当前月份/周数、精力、现金、花呗，方便玩家做路线规划。
  - 底层手机和主界面被遮罩压暗，避免视觉穿透和误点。

已验证：
- Godot headless 加载 `res://scenes/MainGame.tscn` 通过。
- Godot 编辑器错误日志为 0。
- Godot MCP CLI 通道可用，已通过临时截图复测地图：
  - 正常精力：图书馆、健身房、酒吧、宅家等卡片显示正常，按钮可读。
  - 低精力：地点状态正确变成 `精力不足`，按钮变成 `查看原因`。
  - 大屏遮罩正常，底层手机变暗，关闭按钮可见。
- 清理了测试截图产生的 `.import` 垃圾文件，避免污染项目资源。

剩余体验债：
- 地图卡片仍是纯文字型，后续如果继续打磨，可以加入真实地点图标或小地图路线感。
- 酒吧等高阶地点目前一直显示；这是为了保留探索感，但后续可以加“推荐/高风险”标签，而不是直接隐藏。
- 地点收益和随机事件还需要继续数值复盘，地图只解决“看得懂、点得明白”的第一层问题。

## 2026-06-17 第十刀：地点行动闭环修复

本轮目标：
- 地图已经变成周末行动入口，入口之后的地点流程必须稳定。
- 本轮不重写微信 NPC 剧情，只修地点行动地基：背景生命周期、邂逅加微信、结算节奏、好感脏数据。

本轮已改：
- `MainGame.gd`：
  - 左侧对话输入优先判断 Galgame 选择容器。
  - 加微信选项出现后，Ctrl/空格不会再把选项阶段当普通对话跳掉。
- `GalgameSystem.gd`：
  - 加微信选项阶段清空旧 `_gal_pages`，避免旧对话页残留影响输入判断。
  - 同意加微信后，确保 NPC 消息数组存在。
  - 同意加微信后强制重建微信聊天列表、刷新微信 UI、刷新未读状态。
  - 未绑定对象的 `affection` 变化不再落到普通属性。
- `AppPopupSystem.gd`：
  - 新增 `_drop_unbound_affection()`。
  - 地点普通结算会丢弃未绑定 NPC 的好感变化，避免出现“好感+3 但不知道是谁”的假反馈。
  - NPC 结算只在有明确 `npc_id` 时显示/应用好感变化。

已验证：
- `validate_script` 通过：
  - `res://scenes/MainGame.gd`
  - `res://scripts/GalgameSystem.gd`
  - `res://scripts/AppPopupSystem.gd`
- Godot headless 启动 `res://scenes/MainGame.tscn` 通过。
- Godot 编辑器错误日志为 0。
- 健身房阿伟邂逅链路复测：
  - 触发健身房事件后，地点背景 hold=1。
  - 模拟 Ctrl 跳过前置对话，进入加微信选项。
  - 加微信选项出现后继续触发 Ctrl 路径，选项不会被跳掉。
  - 选择同意后，`a_wei` 成为微信联系人，`unlocked=true`，聊天列表里有 `a_wei`。
  - 初始消息/来源消息写入，未读数正常。
  - 地点事件结束后背景回到 home，hold=0。
- 图书馆无邂逅路径复测：
  - 开始时背景 context=location，hold=1。
  - 剧情和结算结束后 context=home，hold=0。
  - 精力和学识在结算后变化。
- 未绑定好感保险复测：
  - 直接对地点结算喂入 `{affection: 3}`，返回空结果，不污染任何 NPC 或全局属性。

剩余体验债：
- 地点随机事件和城市碎片仍需要长线 QA，尤其是带选项的碎片事件。
- 本轮只修“能加上微信/背景不乱跳/好感不脏”，没有重写 NPC 剧情内容。
- 后续应继续补自动化测试脚本，减少手动 CLI 往返测试成本。

## 2026-06-17 第十一刀：支付宝账单与债务可读性

本轮目标：
- 把“钱”和“债”从隐藏数值改成玩家能理解、能复盘、能提前预警的核心系统。
- 不重写经济算法，先补齐流水、月结记录、支付前后果预览和支付宝账单页信息结构。

本轮已改：
- `GameManager.gd`：
  - `add_finance()` 增加流水快照：月份、月内周、年龄、分类、现金余额、未分期花呗、分期债务、总债务。
  - 月底结算逐项写流水：工资、房租、餐饮、分期月供、花呗最低还款、花呗滚息。
  - 月结完成提示的 `net_change` 改成真实现金变化，包含花呗/分期扣款。
- `AlipaySystem.gd`：
  - 支付弹窗显示现金支付和花呗支付的后果。
  - 现金不足时禁用现金支付按钮，并显示差额。
  - 花呗支付按钮明确显示本次透支额。
  - 账单页新增“本月收支汇总”和“月底账单预估”。
  - 流水行显示月份、周数、分类、描述、金额和花呗标记。
  - 理财操作归类为 `理财`，避免混入普通消费统计。
  - 主动还款、分期、支付消费都补充分类。
- `AppPopupSystem.gd`：
  - 深夜冲动消费流水补充 `消费` 分类。

已验证：
- `validate_script` 通过：
  - `res://scripts/AlipaySystem.gd`
  - `res://scripts/GameManager.gd`
  - `res://scripts/AppPopupSystem.gd`
- 运行时测试：
  - 支付 1200 元预览能显示现金支付/花呗支付后果。
  - 月结后生成工资、房租、餐饮、最低还款、滚息流水。
  - 测试数据下现金从 5000 经过工资/账单/最低还款后为 5100，花呗从 4000 最低还款 600 后滚息到 3570。
- 视觉检查：
  - 支付宝大屏可打开，总览 Tab 无明显遮挡。
  - 底层手机被压暗，视觉焦点在支付宝窗口。

剩余体验债：
- 支付宝账单仍是文字列表，后续应升级为真正表格/筛选器。
- 财务流水已经结构化，但 F1 DebugPanel 暂未接入账单审计。
- 经济数值本身还需要继续平衡：最低还款公式、滚息 5%、分期 15% 手续费后续可按体验调整。
